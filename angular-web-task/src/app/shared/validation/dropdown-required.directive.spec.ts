import { DropdownRequiredDirective } from './dropdown-required.directive';

describe('DropdownRequiredDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownRequiredDirective();
    expect(directive).toBeTruthy();
  });
});
