import { CodeExistsDirective } from './code-exists.directive';

describe('CodeExistsDirective', () => {
  it('should create an instance', () => {
    const directive = new CodeExistsDirective();
    expect(directive).toBeTruthy();
  });
});
