import { UsernameExistsDirective } from './username-exists.directive';

describe('UsernameExistsDirective', () => {
  it('should create an instance', () => {
    const directive = new UsernameExistsDirective();
    expect(directive).toBeTruthy();
  });
});
